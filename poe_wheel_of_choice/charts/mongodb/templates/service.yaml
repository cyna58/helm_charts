apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
spec:
  ClusterIP: None
  selector:
    app: {{ .Values.name }}
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
